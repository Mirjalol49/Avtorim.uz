import React from 'react'

const Btn = ({text}) => {
  return (
    
      <a className="button-6" role="button" href="#contact">
                 <span className="text">{text}</span>
    </a>
    
  )
}

export default Btn
